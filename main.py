import impls, config, cv_util, resources

# impl = impls.CommandLineTool()
# print(impl.capture("{}/test.png".format(config.capture_output_path)))
# impl.click(0, 0)
# cv_util.show_img(resources.action_buster)
